<section >


<% if (books) { %>
    <img src='<%=books.image_url %>' alt="books Cover">
    <h2>
        <%= books.title %>
    </h2>
    <h3>
        <%= books.author %>
    </h3>
    <p class="description">
        <%= books.description %>
    </p>
    <p>
        ISBN: <%= books.isbn %>
    </p>
   <p><%= books.offshelf %></p>

             <form action="/books/<%= books.id %>?_method=DELETE" method='POST'>
            <button type="submit">Delete Book</button>
        </form>
      
<button type="button" > UUPDATE DATA  </button>
   <% } %>
</section